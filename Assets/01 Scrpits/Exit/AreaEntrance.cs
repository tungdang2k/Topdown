    using UnityEngine;

public class AreaEntrance : MonoBehaviour
{
    [SerializeField] private string m_sceneTransitionName;

    private void Start()
    {
        //if(SceneController.Instance.SceneTransitionName == m_sceneTransitionName)
        //{
        //    PlayerController.Instance.transform.position = transform.position;
        //}
    }
}
